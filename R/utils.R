#' Change from tibble to matrix format for posterior parameters
#'
#' Parameters of the posterior multivariate t-distributions computed from
#' multi_posterior_mean() are return in a long-tibble format. This
#' function converts results into vector (for the mean) and matrix
#' (for the covariance) format for each \code{Group} and \code{Draw}.
#'
#' @returns
#' @export
#'
#' @examples
tibble_to_matrix = function(){

}
